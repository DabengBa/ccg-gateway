# 根目录创建 .env 文件，即可自定义配置信息；未创建 .env 文件则使用默认值。

# 网关端口（默认7788）
GATEWAY_PORT=7788

# 日志写入文件（默认关闭，设为1开启，日志位于 data/app.log）
LOG_TO_FILE=0

# UI页面端口（默认3000，仅开发环境生效）
UI_PORT=3000

# 网关共享 Token 鉴权（默认开启）
# - CCG_AUTH_ENABLED=true 时：/admin/v1/* 与 /{path:path} 需要携带 Token
# - CCG_AUTH_ENABLED=false 时：关闭鉴权（不建议公网环境使用）
CCG_AUTH_ENABLED=1

# 共享 Token（必填：当 CCG_AUTH_ENABLED=true 时必须提供，否则后端启动会失败）
CCG_AUTH_TOKEN=

# 自定义鉴权头名称（默认 X-CCG-Token）
CCG_AUTH_HEADER_NAME=X-CCG-Token
